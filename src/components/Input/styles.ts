import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    borderColor: "#C3C5CB",
    borderWidth: 1,
    borderRadius: 8,
    padding: 16,
    fontSize: 14,
    backgroundColor: "#FFFFFF",
    width: "100%"
  },
});