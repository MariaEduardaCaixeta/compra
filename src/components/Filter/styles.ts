import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    filterContainer: {
        flexDirection: 'row',
        alignItems: "center",
        gap: 8
    },
    filterText: {
        fontSize: 12,
        fontWeight: '600',
    }
});