import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    rowContainer: {
        flexDirection: 'row',
        gap: 8,
        paddingVertical: 16,
        alignItems: 'center',
    },
    rowText: {
        flex: 1,
        color: '#000',
        fontSize: 14,
        fontWeight: '600',
    }
})