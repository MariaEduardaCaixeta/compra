import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  button: {
    backgroundColor: "#2C46B1",
    paddingVertical: 15,
    borderRadius: 8,
    alignItems: "center",
    width: "100%"
  },
  buttonText: {
    color: "#FFFFFF",
    fontSize: 14,
    fontWeight: "600",
  },
});